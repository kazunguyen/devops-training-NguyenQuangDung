apiVersion: argoproj.io/v1alpha1
kind: EventSource
metadata:
  name: dvc-webhook
  namespace: argo-events
spec:
  service:
    ports:
      - port: 12000
        targetPort: 12000
  webhook:
    dvc-push:
      port: "12000"
      endpoint: /dvc-push
      method: POST